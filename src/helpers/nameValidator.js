export function firstnameValidator(name) {
  if (!name) return "Firstname can't be empty."
  return ''
}

export function lastnameValidator(name) {
  if (!name) return "Lastname can't be empty."
  return ''
}
